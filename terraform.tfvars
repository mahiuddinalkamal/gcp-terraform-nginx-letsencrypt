#project = "" # not needed, passing via script
domain = "dev.codezonebd.com" # change to replace with your own domain name
region = "us-central1"
user = "test-user" # this should match the username set by the OS Login
#email = "" # not needed, passing via script
#privatekeypath = "" # used the default one, change if you need to use a private key from a different directory
#publickeypath = "" # used the default one, change if you need to use a private key from a different directory
